CKEDITOR.editorConfig = function( config )
{
	// Define changes to default configuration here. For example:
	// config.language = 'fr';
	//config.uiColor = '#FF0000';
	//config.contentsCss = '{{=URL('static', 'css', args='main.css', scheme=True, host=True)}}';
};